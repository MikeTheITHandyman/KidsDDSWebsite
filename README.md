# Kids Dentist — Next.js scaffold

This workspace contains a minimal App Router scaffold matching the provided sitemap. Files are placeholders; next steps:

- Install Next.js and dependencies (`npm init`, `npm install next react react-dom`)
- Replace placeholder content with real copy and assets in `public/brand_assets`
- Implement Tailwind CSS and site styles
- Add SEO metadata and structured data per page
